# 上流現行ベンチ結果 (2026-03-08)

## 対象
- 上流現行 `C:\Users\na6ce\source\repos\IndigoMovieManager`
- 入力フォルダ: `D:\BentchItem_HDD`
- ベンチDB: `C:\Users\na6ce\source\repos\IndigoMovieManager\bench\upstream_current_bench.wb`
- ベンチログ: `C:\Users\na6ce\AppData\Local\IndigoMovieManager_bench_upstream_current\logs\bench-runtime.log`

## 実行結果
- `open_datafile_start=1`
- `open_datafile_end=1`
- `scan_start=1`
- `scan_end=1`
- `thumb_start=80`
- `thumb_end=80`

## 取得値
- `movie` テーブル件数: `80`
- `(movie_name, hash)` 一意件数: `79`
- 出力jpg件数: `71`

## 重要観察
- `.swf` は内部では失敗している挙動がある。
- その際、画面上では前回の別動画と思われる画像がバインドされたように見えるケースを確認した。
- これは「SWFが成功した」のではなく、表示更新またはバインド状態が壊れている可能性が高い。
- 結果解釈では、`.swf` の見た目表示を成功判定に使わないこと。

## 判定
- サムネイルキュー処理自体は完了している。
- ただし、出力件数と表示挙動には未解決の差異がある。
- 特に `.swf` は既知の不整合として別枠で扱う。

## 次の調査
- `.swf` 失敗時に、どの `ThumbPath*` がUIへ残留しているか確認する。
- 失敗時に error画像へ確実に切り替わるか確認する。
- 前回別動画jpgの再利用が、キャッシュ・バインド・同名出力のどこで起きるか切り分ける。
