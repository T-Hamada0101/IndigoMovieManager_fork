# 調査結果_再生できないflv_成功条件_2026-03-11

## 結論
- `再生できない.flv` の成功条件をログから固定した。
- 以後はこの条件を再現用の基準値として扱う。

## 対象動画
- `E:\_サムネイル作成困難動画\flv\再生できない.flv`

## ログから確定できた成功条件
- `movieinfo-probe` で以下が出ていること
  - `duration_sec=478.95`
  - `fps=19.938`
  - `frames=9549`
  - `codec='flv1'`
- `autogen success=True`
- `detail='format_duration'`

## 成功経路
- `repair` ではない
- `ffmpeg1pass` ではない
- `Normal worker + autogen` の直通成功

## 確認できたログ
- `thumbnail-create-process.csv`
  - `2026-03-11 02:33:43.976` に `autogen success`
  - `2026-03-11 02:41:23.164` に `autogen success`

## 補足
- retained log 上では、この動画は `2026-03-06` 以降にも成功履歴がある。
- そのため、今回の成功は「履歴上の初成功」ではない。

## 再現手順メモ
1. 対象動画 `E:\_サムネイル作成困難動画\flv\再生できない.flv` を処理対象に置く。
2. `movieinfo-probe` の結果が以下と一致するか確認する。
   - `duration_sec=478.95`
   - `fps=19.938`
   - `frames=9549`
   - `codec='flv1'`
3. 実行経路が `repair` / `ffmpeg1pass` に落ちず、`Normal worker + autogen` 直通になることを確認する。
4. `thumbnail-create-process.csv` に `autogen success` が出ることを確認する。
5. 判定に迷った場合は、`detail='format_duration'` が付いているかも合わせて見る。

## 次の調査論点
- なぜ `autogen` 直通で抜ける時と抜けない時があるのかを、コード条件へ落とし込む。
