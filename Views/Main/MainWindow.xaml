<Window
  x:Class="IndigoMovieManager.MainWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:avalonDock="https://github.com/Dirkster99/AvalonDock"
  xmlns:c="clr-namespace:IndigoMovieManager.Converter"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:IndigoMovieManager"
  xmlns:materialDesign="clr-namespace:MaterialDesignThemes.Wpf"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:notifications="clr-namespace:Notification.Wpf.Controls;assembly=Notification.Wpf"
  xmlns:prop="clr-namespace:IndigoMovieManager.Properties"
  xmlns:bookmarkTab="clr-namespace:IndigoMovieManager.BottomTabs.Bookmark"
  xmlns:extensionTab="clr-namespace:IndigoMovieManager.BottomTabs.Extension"
  xmlns:debugTab="clr-namespace:IndigoMovieManager.BottomTabs.Debug"
  xmlns:logTab="clr-namespace:IndigoMovieManager.BottomTabs.Log"
  xmlns:savedSearchTab="clr-namespace:IndigoMovieManager.BottomTabs.SavedSearch"
  xmlns:tagEditorTab="clr-namespace:IndigoMovieManager.BottomTabs.TagEditor"
  xmlns:thumbnailErrorTab="clr-namespace:IndigoMovieManager.BottomTabs.ThumbnailError"
  xmlns:thumbnailProgressTab="clr-namespace:IndigoMovieManager.BottomTabs.ThumbnailProgress"
  xmlns:upperTabsCommon="clr-namespace:IndigoMovieManager.UpperTabs.Common"
  xmlns:rescueTab="clr-namespace:IndigoMovieManager.UpperTabs.Rescue"
  xmlns:duplicateVideosTab="clr-namespace:IndigoMovieManager.UpperTabs.DuplicateVideos"
  xmlns:view="clr-namespace:IndigoMovieManager.UserControls"
  xmlns:vwp="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
  xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
  x:Name="window"
  Title="Indigo Movie Manager"
  AllowDrop="True"
  Width="800"
  Height="Auto"
  PreviewDragOver="MainWindow_PreviewDragOver"
  Drop="MainWindow_Drop"
  mc:Ignorable="d"
>
  <Window.Resources>
    <c:FileSizeConverter x:Key="FileSizeConverter" />
    <c:NoLockImageConverter x:Key="noLockImageConverter" />
    <c:TagSummaryConverter x:Key="tagSummaryConverter" />
    <c:ThumbnailProgressPreviewConverter x:Key="thumbnailProgressPreviewConverter" />
    <upperTabsCommon:UpperTabImageSourceConverter x:Key="upperTabImageSourceConverter" />
    <Style x:Key="UpperTabListCellTextStyle" TargetType="TextBlock">
      <Setter Property="Foreground" Value="{DynamicResource UpperTabPanelForegroundBrush}" />
    </Style>
    <Style
      x:Key="UpperTabListRightAlignedCellTextStyle"
      BasedOn="{StaticResource UpperTabListCellTextStyle}"
      TargetType="TextBlock"
    >
      <Setter Property="HorizontalAlignment" Value="Right" />
    </Style>
    <Style
      x:Key="UpperTabListWrappedCellTextStyle"
      BasedOn="{StaticResource UpperTabListCellTextStyle}"
      TargetType="TextBlock"
    >
      <Setter Property="TextWrapping" Value="Wrap" />
    </Style>
    <Style
      x:Key="LeftDrawerButtonStyle"
      BasedOn="{StaticResource AppDrawerButtonStyle}"
      TargetType="Button"
    >
      <Setter Property="Height" Value="30" />
      <Setter Property="MinHeight" Value="30" />
      <Setter Property="Margin" Value="0,1" />
      <Setter Property="Padding" Value="8,0" />
      <Setter Property="HorizontalAlignment" Value="Stretch" />
      <Setter Property="HorizontalContentAlignment" Value="Left" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="FontWeight" Value="Normal" />
      <Setter Property="Foreground" Value="{DynamicResource LeftDrawerForegroundBrush}" />
    </Style>
    <Style x:Key="LeftDrawerTreeItemStyle" TargetType="TreeViewItem">
      <Setter Property="IsExpanded" Value="{Binding Path=IsExpanded, Mode=TwoWay}" />
      <Setter Property="Margin" Value="0" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>
    <Style x:Key="LeftDrawerTextStyle" TargetType="TextBlock">
      <Setter Property="VerticalAlignment" Value="Center" />
      <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>
    <Style x:Key="LeftDrawerSeparatorStyle" TargetType="Border">
      <Setter Property="Height" Value="1" />
      <Setter Property="Margin" Value="0,8" />
      <Setter Property="Background" Value="{DynamicResource AppBorderBrush}" />
      <Setter Property="Opacity" Value="0.7" />
    </Style>
    <!--  BoolToVisibilityConverter を追加  -->
    <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />

    <ContextMenu x:Key="menuContext" FontSize="10" Opened="MenuContext_Opened">
      <MenuItem
        x:Name="PlayDefault"
        Click="PlayMovie_Click"
        Header="プレイヤーから開く(_E)"
        Icon="{materialDesign:PackIcon Kind=MoviePlay}"
        InputGestureText="ENTER"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="PlayFromThumb"
        Click="PlayMovie_Click"
        Header="サムネイルから開く(_H)"
        Icon="{materialDesign:PackIcon Kind=MoviePlay}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <Separator />
      <MenuItem
        x:Name="TagEdit"
        Click="TagEdit_Click"
        Header="タグ編集(_T)"
        Icon="{materialDesign:PackIcon Kind=Edit}"
        InputGestureText="F6"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="TagAdd"
        Click="TagAdd_Click"
        Header="タグ追加(_A)"
        Icon="{materialDesign:PackIcon Kind=AttachmentAdd}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="TagDelete"
        Click="TagDelete_Click"
        Header="タグ削除(_V)"
        Icon="{materialDesign:PackIcon Kind=RemoveBox}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="TagCopy"
        Click="TagCopy_Click"
        Header="タグコピー(_C)"
        Icon="{materialDesign:PackIcon Kind=ContentCopy}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="TagPaste"
        Click="TagPaste_Click"
        Header="タグ貼り付け(_V)"
        Icon="{materialDesign:PackIcon Kind=ContentPaste}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="ScorePlus"
        Click="MenuScore_Click"
        Header="スコア +1 (_A)"
        Icon="{materialDesign:PackIcon Kind=Plus}"
        InputGestureText="+"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="ScoreMinus"
        Click="MenuScore_Click"
        Header="スコア -1 (_S)"
        Icon="{materialDesign:PackIcon Kind=Minus}"
        InputGestureText="-"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <Separator />
      <MenuItem
        Click="CreateThumb_EqualInterval"
        Header="等間隔サムネイル作成(_I)"
        Icon="{materialDesign:PackIcon Kind=Create}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="SendToThumbnailRescueTabMenu"
        Click="SendToThumbnailRescueTabMenu_Click"
        Header="サムネ救済タブへ送る(_T)"
        Icon="{materialDesign:PackIcon Kind=ImageRefresh}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <!--- ランダムサムネイルと画像からはなしにした
                                                <MenuItem Header="ランダムサムネイル作成(_R)" Icon="{materialDesign:PackIcon Kind=CreateOutline}" />
                                                <MenuItem Header="画像からサムネイル作成(_P)" Icon="{materialDesign:PackIcon Kind=Image}" />
            -->
      <MenuItem
        x:Name="ManualThumbnail"
        Click="ManualThumbnail_Click"
        Header="マニュアルサムネイル作成(_U)"
        Icon="{materialDesign:PackIcon Kind=Manufacturing}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="ThumbnailRescueMenu"
        Click="ThumbnailRescueMenu_Click"
        Header="サムネイル救済(_R)"
        Icon="{materialDesign:PackIcon Kind=ImageRefresh}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="ThumbnailDarkHeavyBackgroundRescueMenu"
        Click="ThumbnailDarkHeavyBackgroundRescueMenu_Click"
        Header="黒多め背景救済(_K)"
        Icon="{materialDesign:PackIcon Kind=ImageFilterBlackWhite}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="ThumbnailDarkHeavyBackgroundLiteRescueMenu"
        Click="ThumbnailDarkHeavyBackgroundLiteRescueMenu_Click"
        Header="黒多め背景救済Lite(_L)"
        Icon="{materialDesign:PackIcon Kind=ImageOutline}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="ThumbnailIndexRepairMenu"
        Click="ThumbnailIndexRepairMenu_Click"
        Header="インデックス再構築(_I)"
        Icon="{materialDesign:PackIcon Kind=AutoFix}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <Separator />
      <MenuItem
        x:Name="DeleteMovie"
        Click="DeleteMovieRecord_Click"
        Header="登録から削除(_D)"
        Icon="{materialDesign:PackIcon Kind=Delete}"
        InputGestureText="DELETE"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="FileCopy"
        Click="MenuCopyAndMove_Click"
        Header="ファイルをコピー(_C)"
        Icon="{materialDesign:PackIcon Kind=ContentCopy}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="CopyPath"
        Click="CopyPath_Click"
        Header="パスのコピー(_P)"
        Icon="{materialDesign:PackIcon Kind=FileDocumentOutline}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="FileMove"
        Click="MenuCopyAndMove_Click"
        Header="ファイルを移動(_M)"
        Icon="{materialDesign:PackIcon Kind=MoveToInbox}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="DeleteFile"
        Click="DeleteMovieRecord_Click"
        Header="ファイルを削除(_L)"
        Icon="{materialDesign:PackIcon Kind=RemoveCircle}"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <MenuItem
        x:Name="RenameFile"
        Click="RenameFile_Click"
        Header="ファイル名変更(_N)"
        Icon="{materialDesign:PackIcon Kind=Rename}"
        InputGestureText="F2"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <Separator />
      <MenuItem
        Click="OpenParentFolder_Click"
        Header="親フォルダを開く(_F)"
        Icon="{materialDesign:PackIcon Kind=FolderOpen}"
        InputGestureText="F12"
      >
        <MenuItem.Style>
          <Style TargetType="MenuItem">
            <Setter Property="Padding" Value="1" />
          </Style>
        </MenuItem.Style>
      </MenuItem>
      <!-- この辺はウィンドウ作らないといけないので劣後だろうな。
                                                <MenuItem Header="プロパティ(_P)" Icon="{materialDesign:PackIcon Kind=PropertyTag}" InputGestureText="P">
                                                    <MenuItem.Style>
                                                        <Style TargetType="MenuItem">
                                                            <Setter Property="Padding" Value="1" />
                                                        </Style>
                                                    </MenuItem.Style>
                                                </MenuItem>
            -->
    </ContextMenu>
  </Window.Resources>

  <Window.Background>
    <DynamicResource ResourceKey="MaterialDesignPaper" />
  </Window.Background>

  <Grid x:Name="MainWindowRoot">
    <avalonDock:DockingManager x:Name="uxDockingManager" Margin="0,48,0,0">
      <avalonDock:DockingManager.Theme>
        <DynamicResource ResourceKey="AvalonDockTheme" />
      </avalonDock:DockingManager.Theme>
      <avalonDock:LayoutRoot>
        <avalonDock:LayoutPanel Orientation="Vertical">
          <avalonDock:LayoutPanel
            CanDock="False"
            CanRepositionItems="False"
            Orientation="Horizontal"
          >
            <avalonDock:LayoutDocumentPane
              x:Name="uxDocumentPane"
              CanRepositionItems="False"
              ShowHeader="False"
            >
              <!--  ドキュメント  ★ここから  -->
              <avalonDock:LayoutDocument Title="Document1" ContentId="Document1">
                <Grid>
                  <TabControl
                    x:Name="Tabs"
                    Margin="0,0,0,0"
                    PreviewKeyDown="Tab_PreviewKeyDown"
                    SelectionChanged="Tabs_SelectionChangedAsync"
                  >
                    <TabControl.Resources>
                      <Style TargetType="TabItem" BasedOn="{StaticResource AppTabItemStyle}">
                        <Setter Property="Height" Value="36" />
                        <Setter Property="Padding" Value="16,6,16,6" />
                      </Style>
                    </TabControl.Resources>
                    <TabItem x:Name="TabSmall" Background="{DynamicResource UpperTabBackground}" Header="Small" Tag="120x90x3x1">
                    <ListView
                      x:Name="SmallList"
                      Height="Auto"
                      ItemsSource="{Binding FilteredMovieRecs, UpdateSourceTrigger=PropertyChanged}"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.IsDeferredScrollingEnabled="True"
                      SelectionChanged="List_SelectionChanged"
                      SelectionMode="Extended"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                    >
                      <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                          <vwp:VirtualizingWrapPanel
                            Orientation="Horizontal"
                            SpacingMode="None"
                            StretchItems="False"
                            VirtualizingPanel.IsVirtualizing="True"
                            VirtualizingPanel.VirtualizationMode="Recycling"
                          />
                        </ItemsPanelTemplate>
                      </ListView.ItemsPanel>

                      <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                          <!--  内容更新時のバインドエラー回避のおまじない。  -->
                          <Setter Property="Margin" Value="0" />
                          <Setter Property="Padding" Value="0" />
                          <Setter Property="Background" Value="Transparent" />
                          <Setter Property="BorderBrush" Value="Transparent" />
                          <Setter Property="BorderThickness" Value="0" />
                          <Setter Property="HorizontalAlignment" Value="Left" />
                          <Setter Property="HorizontalContentAlignment" Value="Left" />
                          <Setter Property="VerticalContentAlignment" Value="Top" />
                          <!--  ここを追加: ListViewItem内のどこをクリックしても選択状態にする  -->
                          <EventSetter
                            Event="PreviewMouseLeftButtonDown"
                            Handler="SmallListItem_PreviewMouseLeftButtonDown"
                          />
                        </Style>
                      </ListView.ItemContainerStyle>

                      <ListView.ItemTemplate>
                        <DataTemplate>
                          <StackPanel>
                            <StackPanel Orientation="Horizontal">
                              <Label
                                Width="288"
                                Height="72"
                                Padding="0"
                                BorderThickness="0"
                                Background="{DynamicResource ThumbImageBackground}"
                                MouseDoubleClick="PlayMovie_Click"
                                MouseDown="Label_MouseDown"
                              >
                                <Image ContextMenu="{StaticResource menuContext}" Stretch="Uniform">
                                  <Image.Source>
                                    <MultiBinding
                                      Converter="{StaticResource upperTabImageSourceConverter}"
                                      ConverterParameter="{x:Static upperTabsCommon:UpperTabDecodeProfile.SmallDecodePixelHeight}"
                                    >
                                      <Binding Path="ThumbPathSmall" />
                                      <Binding Path="IsExists" />
                                      <Binding Source="{x:Reference SmallList}" Path="IsVisible" />
                                      <Binding Path="Movie_Path" />
                                      <Binding Source="{x:Reference window}" Path="UpperTabPreferredMoviePathKeysRevision" />
                                    </MultiBinding>
                                  </Image.Source>
                                </Image>
                              </Label>

                              <view:SmallDetailControl VerticalAlignment="Top" />
                            </StackPanel>

                            <ItemsControl
                              Height="Auto"
                              ItemsSource="{Binding Tag, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"
                              Width="439"
                            >
                              <ItemsControl.Style>
                                <Style TargetType="ItemsControl">
                                  <Setter Property="Visibility" Value="Collapsed" />
                                  <Style.Triggers>
                                    <Trigger Property="HasItems" Value="True">
                                      <Setter Property="Visibility" Value="Visible" />
                                    </Trigger>
                                  </Style.Triggers>
                                </Style>
                              </ItemsControl.Style>
                              <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                  <view:TagControl DataContext="{Binding}" />
                                </DataTemplate>
                              </ItemsControl.ItemTemplate>

                              <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                  <WrapPanel
                                    Width="439"
                                    Height="Auto"
                                    MaxHeight="110"
                                  />
                                </ItemsPanelTemplate>
                              </ItemsControl.ItemsPanel>
                            </ItemsControl>
                          </StackPanel>
                        </DataTemplate>
                      </ListView.ItemTemplate>
                    </ListView>
                  </TabItem>

                  <TabItem x:Name="TabBig" Background="{DynamicResource UpperTabBackground}" Header="Big" Tag="200x150x3x1">
                    <ListView
                      x:Name="BigList"
                      ItemsSource="{Binding FilteredMovieRecs, UpdateSourceTrigger=PropertyChanged}"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.IsDeferredScrollingEnabled="True"
                      SelectionChanged="List_SelectionChanged"
                      SelectionMode="Extended"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.ScrollUnit="Pixel"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                    >
                      <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                          <vwp:VirtualizingWrapPanel
                            Orientation="Horizontal"
                            StretchItems="True"
                            VirtualizingPanel.IsVirtualizing="True"
                            VirtualizingPanel.ScrollUnit="Pixel"
                            VirtualizingPanel.VirtualizationMode="Recycling"
                          />
                        </ItemsPanelTemplate>
                      </ListView.ItemsPanel>

                      <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                          <!--  内容更新時のバインドエラー回避のおまじない。  -->
                          <Setter Property="HorizontalContentAlignment" Value="Left" />
                          <Setter Property="VerticalContentAlignment" Value="Stretch" />
                          <!--  ここを追加: ListViewItem内のどこをクリックしても選択状態にする  -->
                          <EventSetter
                            Event="PreviewMouseLeftButtonDown"
                            Handler="BigListItem_PreviewMouseLeftButtonDown"
                          />
                        </Style>
                      </ListView.ItemContainerStyle>

                      <ListView.ItemTemplate>
                        <DataTemplate>
                          <VirtualizingStackPanel
                            Orientation="Horizontal"
                            ScrollViewer.IsDeferredScrollingEnabled="true"
                            VirtualizingPanel.IsVirtualizing="True"
                            VirtualizingPanel.VirtualizationMode="Recycling"
                          >
                            <VirtualizingStackPanel
                              Orientation="Vertical"
                              VirtualizingPanel.IsVirtualizing="True"
                              VirtualizingPanel.ScrollUnit="Pixel"
                              VirtualizingPanel.VirtualizationMode="Recycling"
                            >
                              <view:BigTitleControl DataContext="{Binding}" />

                              <Label
                                Width="600"
                                Height="150"
                                MouseDoubleClick="PlayMovie_Click"
                                MouseDown="Label_MouseDown"
                              >
                                <Image
                                  ContextMenu="{StaticResource menuContext}"
                                  Stretch="Uniform"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.ScrollUnit="Pixel"
                                  VirtualizingPanel.VirtualizationMode="Recycling"
                                >
                                  <Image.Source>
                                    <MultiBinding
                                      Converter="{StaticResource upperTabImageSourceConverter}"
                                      ConverterParameter="{x:Static upperTabsCommon:UpperTabDecodeProfile.BigDecodePixelHeight}"
                                    >
                                      <Binding Path="ThumbPathBig" />
                                      <Binding Path="IsExists" />
                                      <Binding Source="{x:Reference BigList}" Path="IsVisible" />
                                      <Binding Path="Movie_Path" />
                                      <Binding Source="{x:Reference window}" Path="UpperTabPreferredMoviePathKeysRevision" />
                                    </MultiBinding>
                                  </Image.Source>
                                </Image>
                              </Label>
                            </VirtualizingStackPanel>

                            <view:BigDetailControl DataContext="{Binding}" />
                          </VirtualizingStackPanel>
                        </DataTemplate>
                      </ListView.ItemTemplate>
                    </ListView>
                  </TabItem>

                  <TabItem x:Name="TabGrid" Background="{DynamicResource UpperTabBackground}" Header="Grid" Tag="160x120x1x1">
                    <ListView
                      x:Name="GridList"
                      ItemsSource="{Binding FilteredMovieRecs, UpdateSourceTrigger=PropertyChanged}"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.IsDeferredScrollingEnabled="True"
                      SelectionChanged="List_SelectionChanged"
                      SelectionMode="Extended"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                    >
                      <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                          <vwp:VirtualizingWrapPanel
                            Orientation="Horizontal"
                            SpacingMode="None"
                            StretchItems="False"
                            VirtualizingPanel.IsVirtualizing="True"
                          />
                        </ItemsPanelTemplate>
                      </ListView.ItemsPanel>

                      <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                          <!--  内容更新時のバインドエラー回避のおまじない。  -->
                          <Setter Property="Margin" Value="0" />
                          <Setter Property="Padding" Value="0" />
                          <Setter Property="HorizontalAlignment" Value="Left" />
                          <Setter Property="HorizontalContentAlignment" Value="Left" />
                          <Setter Property="VerticalContentAlignment" Value="Stretch" />
                        </Style>
                      </ListView.ItemContainerStyle>

                      <ListView.ItemTemplate>
                        <DataTemplate>
                          <Label
                            Width="160"
                            Height="120"
                            Padding="0"
                            BorderThickness="0"
                            ContextMenu="{StaticResource menuContext}"
                            MouseDoubleClick="PlayMovie_Click"
                            MouseDown="Label_MouseDown"
                          >
                            <Grid ClipToBounds="True">
                              <Image
                                Stretch="Uniform"
                                VirtualizingPanel.IsVirtualizing="True"
                                VirtualizingPanel.ScrollUnit="Pixel"
                                VirtualizingPanel.VirtualizationMode="Recycling"
                              >
                                <Image.Source>
                                  <MultiBinding
                                    Converter="{StaticResource upperTabImageSourceConverter}"
                                    ConverterParameter="{x:Static upperTabsCommon:UpperTabDecodeProfile.GridDecodePixelHeight}"
                                  >
                                    <Binding Path="ThumbPathGrid" />
                                    <Binding Path="IsExists" />
                                    <Binding Source="{x:Reference GridList}" Path="IsVisible" />
                                    <Binding Path="Movie_Path" />
                                    <Binding Source="{x:Reference window}" Path="UpperTabPreferredMoviePathKeysRevision" />
                                  </MultiBinding>
                                </Image.Source>
                              </Image>

                              <Border
                                VerticalAlignment="Bottom"
                                Background="#99000000"
                                Padding="4,2"
                                IsHitTestVisible="False"
                              >
                                <TextBlock
                                  FontSize="11"
                                  Foreground="{DynamicResource GridTabTitleForegroundBrush}"
                                  Text="{Binding Movie_Name, UpdateSourceTrigger=PropertyChanged}"
                                  TextTrimming="CharacterEllipsis"
                                  ToolTip="{Binding Movie_Path}"
                                />
                              </Border>
                            </Grid>
                          </Label>
                        </DataTemplate>
                      </ListView.ItemTemplate>
                    </ListView>
                  </TabItem>

                  <TabItem x:Name="TabPlayer" Header="プレイヤー" Tag="player-grid-strip">
                    <Grid x:Name="PlayerTabLayoutRoot" Margin="8,8,8,12" SizeChanged="PlayerTabLayoutRoot_SizeChanged">
                      <Grid.RowDefinitions>
                        <RowDefinition x:Name="PlayerTabTopRow" Height="*" />
                        <RowDefinition x:Name="PlayerTabGapRow" Height="0" />
                        <RowDefinition x:Name="PlayerTabBottomRow" Height="0" />
                      </Grid.RowDefinitions>
                      <Grid.ColumnDefinitions>
                        <ColumnDefinition x:Name="PlayerTabMainColumn" Width="*" />
                        <ColumnDefinition x:Name="PlayerTabGapColumn" Width="12" />
                        <ColumnDefinition x:Name="PlayerTabSideColumn" Width="264" />
                      </Grid.ColumnDefinitions>

                      <Border
                        x:Name="PlayerSurfaceHost"
                        Grid.Row="0"
                        Grid.Column="0"
                        Padding="8"
                        Background="{DynamicResource MaterialDesignPaper}"
                        BorderBrush="{DynamicResource MaterialDesignDivider}"
                        BorderThickness="1"
                        CornerRadius="6"
                      >
                        <Grid>
                          <Grid
                            x:Name="PlayerArea"
                            HorizontalAlignment="Stretch"
                            VerticalAlignment="Stretch"
                            Visibility="Collapsed"
                          >
                            <Grid.RowDefinitions>
                              <RowDefinition Height="*" />
                              <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.Effect>
                              <DropShadowEffect
                                BlurRadius="2"
                                RenderingBias="Performance"
                                ShadowDepth="10"
                                Color="{DynamicResource PlayerShadowColor}"
                              />
                            </Grid.Effect>
                            <MediaElement
                              x:Name="uxVideoPlayer"
                              Grid.Row="0"
                              LoadedBehavior="Manual"
                              MediaEnded="UxVideoPlayer_MediaEnded"
                              MediaFailed="UxVideoPlayer_MediaFailed"
                              MediaOpened="UxVideoPlayer_MediaOpened"
                              PreviewMouseDown="UxVideoPlayer_PreviewMouseDown"
                              ScrubbingEnabled="true"
                              Stretch="Uniform"
                              UnloadedBehavior="Stop"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                            />
                            <wv2:WebView2
                              x:Name="uxWebVideoPlayer"
                              Grid.Row="0"
                              HorizontalAlignment="Stretch"
                              NavigationCompleted="UxWebVideoPlayer_NavigationCompleted"
                              NavigationStarting="UxWebVideoPlayer_NavigationStarting"
                              VerticalAlignment="Stretch"
                              Visibility="Collapsed"
                            />
                            <StackPanel
                              x:Name="PlayerController"
                              Grid.Row="1"
                              HorizontalAlignment="Stretch"
                              Background="{DynamicResource PlayerControllerBackground}"
                              Orientation="Horizontal"
                              Visibility="Collapsed"
                            >
                              <RepeatButton
                                Margin="5"
                                Background="{x:Null}"
                                BorderThickness="0"
                                Click="FR_Click"
                                Content="{materialDesign:PackIcon Kind=Rewind}"
                                Style="{StaticResource AppIconRepeatButtonStyle}"
                                ToolTip="戻す"
                              />
                                <Button
                                x:Name="PlayButton"
                                Margin="5"
                                Background="{x:Null}"
                                BorderThickness="0"
                                Click="Start_Click"
                                Content="{materialDesign:PackIcon Kind=Play}"
                                Style="{StaticResource AppIconButtonStyle}"
                                ToolTip="再生"
                              />
                              <RepeatButton
                                Margin="5"
                                Background="{x:Null}"
                                BorderThickness="0"
                                Click="FF_Click"
                                Content="{materialDesign:PackIcon Kind=FastForward}"
                                Style="{StaticResource AppIconRepeatButtonStyle}"
                                ToolTip="送る"
                              />
                              <Button
                                Margin="5"
                                Background="{x:Null}"
                                BorderThickness="0"
                                Click="Pause_Click"
                                Content="{materialDesign:PackIcon Kind=PauseBox}"
                                Style="{StaticResource AppIconButtonStyle}"
                                ToolTip="一時停止"
                              />
                              <Button
                                Margin="5"
                                Background="{x:Null}"
                                BorderThickness="0"
                                Click="Capture_Click"
                                Content="{materialDesign:PackIcon Kind=Camera}"
                                Style="{StaticResource AppIconButtonStyle}"
                                ToolTip="サムネイルの取得"
                              />
                              <Button
                                Margin="5"
                                Background="{x:Null}"
                                BorderThickness="0"
                                Click="AddBookmark_Click"
                                Content="{materialDesign:PackIcon Kind=BookmarkAdd}"
                                Style="{StaticResource AppIconButtonStyle}"
                                ToolTip="ブックマークの追加"
                              />
                              <Slider
                                x:Name="uxTimeSlider"
                                Width="300"
                                Height="20"
                                Margin="5"
                                IsSnapToTickEnabled="True"
                                LostMouseCapture="UxTimeSlider_LostMouseCapture"
                                PreviewMouseLeftButtonDown="UxTimeSlider_PreviewMouseLeftButtonDown"
                                PreviewMouseLeftButtonUp="UxTimeSlider_PreviewMouseLeftButtonUp"
                                SmallChange="2000"
                                ToolTip="再生位置"
                                ValueChanged="UxTimeSlider_ValueChanged"
                                Value="0"
                              />
                              <TextBlock
                                x:Name="uxTime"
                                Width="Auto"
                                Margin="20,0,0,0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Foreground="{DynamicResource HeaderForeground}"
                                ToolTip="再生時刻"
                              />
                              <Slider
                                x:Name="uxVolumeSlider"
                                Width="50"
                                Height="20"
                                MinWidth="50"
                                Margin="20,0,0,0"
                                Maximum="1"
                                Minimum="0"
                                ToolTip="音量調整"
                                ValueChanged="UxVolumeSlider_ValueChanged"
                                Value="0.5"
                              />
                              <TextBlock
                                x:Name="uxVolume"
                                Width="40"
                                MinWidth="40"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Foreground="{DynamicResource HeaderForeground}"
                                TextAlignment="Right"
                                ToolTip="音量"
                              />
                              <Button
                                Width="30"
                                Height="30"
                                Margin="20,0,0,0"
                                Padding="0"
                                Background="{x:Null}"
                                BorderThickness="1"
                                Click="Stop_Click"
                                Content="{materialDesign:PackIcon Kind=Close}"
                                Style="{StaticResource AppIconButtonStyle}"
                                ToolTip="終了"
                              />
                            </StackPanel>
                          </Grid>

                          <Border
                            x:Name="PlayerEmptyState"
                            Padding="24"
                            Background="{DynamicResource MaterialDesignPaper}"
                            BorderBrush="{DynamicResource MaterialDesignDivider}"
                            BorderThickness="0"
                            CornerRadius="6"
                            Visibility="Visible"
                          >
                            <StackPanel VerticalAlignment="Center">
                              <materialDesign:PackIcon
                                Width="56"
                                Height="56"
                                HorizontalAlignment="Center"
                                Kind="MovieOpenPlay"
                                Opacity="0.75"
                              />
                              <TextBlock
                                Margin="0,16,0,0"
                                HorizontalAlignment="Center"
                                FontSize="20"
                                FontWeight="SemiBold"
                                Foreground="{DynamicResource MaterialDesignBody}"
                                Text="プレイヤー"
                              />
                              <TextBlock
                                Margin="0,8,0,0"
                                HorizontalAlignment="Center"
                                Foreground="{DynamicResource MaterialDesignBody}"
                                Opacity="0.8"
                                Text="右のサムネイルを選ぶと、ここで再生します"
                                TextAlignment="Center"
                                TextWrapping="Wrap"
                              />
                            </StackPanel>
                          </Border>
                        </Grid>
                      </Border>

                      <Border
                        x:Name="PlayerThumbnailHost"
                        Grid.Row="0"
                        Grid.Column="2"
                        Padding="6"
                        Background="{DynamicResource MaterialDesignPaper}"
                        BorderBrush="{DynamicResource MaterialDesignDivider}"
                        BorderThickness="1"
                        CornerRadius="6"
                      >
                        <DockPanel LastChildFill="True">
                          <TextBlock
                            DockPanel.Dock="Top"
                            Margin="0,0,0,8"
                            VerticalAlignment="Center"
                            FontSize="13"
                            FontWeight="SemiBold"
                            Foreground="{DynamicResource MaterialDesignBody}"
                            Text="Grid サムネ"
                          />

                          <ListView
                            x:Name="PlayerThumbnailList"
                            Width="252"
                            HorizontalAlignment="Left"
                            ItemsSource="{Binding FilteredMovieRecs, UpdateSourceTrigger=PropertyChanged}"
                            ScrollViewer.CanContentScroll="True"
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                            ScrollViewer.IsDeferredScrollingEnabled="True"
                            SelectionChanged="PlayerThumbnailList_SelectionChanged"
                            SelectionMode="Extended"
                            VirtualizingPanel.IsVirtualizing="True"
                            VirtualizingPanel.ScrollUnit="Pixel"
                            VirtualizingPanel.VirtualizationMode="Recycling"
                          >
                            <ListView.ItemsPanel>
                              <ItemsPanelTemplate>
                                <vwp:VirtualizingWrapPanel
                                  Orientation="Horizontal"
                                  SpacingMode="None"
                                  StretchItems="False"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.ScrollUnit="Pixel"
                                  VirtualizingPanel.VirtualizationMode="Recycling"
                                />
                              </ItemsPanelTemplate>
                            </ListView.ItemsPanel>

                            <ListView.ItemContainerStyle>
                              <Style TargetType="ListViewItem">
                                <!--  右レールはGrid風の固定幅だけにして、切替と再計算を持ち込まない。  -->
                                <Setter Property="Width" Value="74" />
                                <Setter Property="Margin" Value="0,0,4,8" />
                                <Setter Property="Padding" Value="0" />
                                <Setter Property="Background" Value="Transparent" />
                                <Setter Property="BorderBrush" Value="Transparent" />
                                <Setter Property="BorderThickness" Value="0" />
                                <Setter Property="HorizontalContentAlignment" Value="Left" />
                                <Setter Property="VerticalContentAlignment" Value="Top" />
                                <Setter Property="Template">
                                  <Setter.Value>
                                    <ControlTemplate TargetType="ListViewItem">
                                      <Border
                                        x:Name="ItemChrome"
                                        Padding="{TemplateBinding Padding}"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        BorderThickness="1"
                                        SnapsToDevicePixels="True"
                                      >
                                        <ContentPresenter
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                        />
                                      </Border>
                                      <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                          <Setter TargetName="ItemChrome" Property="Background" Value="#123A6E8A" />
                                          <Setter TargetName="ItemChrome" Property="BorderBrush" Value="#803A9AD9" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                          <Setter TargetName="ItemChrome" Property="Background" Value="#203A6E8A" />
                                          <Setter TargetName="ItemChrome" Property="BorderBrush" Value="#FF3A9AD9" />
                                        </Trigger>
                                      </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                  </Setter.Value>
                                </Setter>
                              </Style>
                            </ListView.ItemContainerStyle>

                            <ListView.ItemTemplate>
                              <DataTemplate>
                                <Border
                                  Width="70"
                                  Padding="3"
                                  Background="{DynamicResource MaterialDesignCardBackground}"
                                  BorderBrush="{DynamicResource MaterialDesignDivider}"
                                  BorderThickness="1"
                                  CornerRadius="4"
                                  SnapsToDevicePixels="True"
                                >
                                  <StackPanel>
                                    <Label
                                      Width="62"
                                      HorizontalAlignment="Stretch"
                                      Height="42"
                                      Padding="0"
                                      BorderThickness="0"
                                      Background="{DynamicResource ThumbImageBackground}"
                                      ContextMenu="{StaticResource menuContext}"
                                      MouseDown="Label_MouseDown"
                                    >
                                      <Image SnapsToDevicePixels="True" Stretch="UniformToFill">
                                        <Image.Source>
                                          <MultiBinding
                                            Converter="{StaticResource upperTabImageSourceConverter}"
                                            ConverterParameter="{x:Static upperTabsCommon:UpperTabDecodeProfile.GridDecodePixelHeight}"
                                          >
                                            <Binding Path="ThumbPathGrid" />
                                            <Binding Path="IsExists" />
                                            <Binding Source="{x:Reference PlayerThumbnailList}" Path="IsVisible" />
                                            <Binding Path="Movie_Path" />
                                            <Binding Source="{x:Reference window}" Path="UpperTabPreferredMoviePathKeysRevision" />
                                          </MultiBinding>
                                        </Image.Source>
                                      </Image>
                                    </Label>
                                    <TextBlock
                                      Margin="0,4,0,0"
                                      FontSize="10"
                                      Foreground="{DynamicResource MaterialDesignBody}"
                                      Text="{Binding Movie_Name, UpdateSourceTrigger=PropertyChanged}"
                                      TextAlignment="Center"
                                      TextTrimming="CharacterEllipsis"
                                    />
                                  </StackPanel>
                                </Border>
                              </DataTemplate>
                            </ListView.ItemTemplate>
                          </ListView>
                        </DockPanel>
                      </Border>
                    </Grid>
                  </TabItem>

                  <TabItem x:Name="TabList" Header="List" Tag="56x42x5x1">
                    <DataGrid
                      x:Name="ListDataGrid"
                      AutoGenerateColumns="False"
                      BeginningEdit="ListDataGrid_BeginningEdit"
                      CanUserAddRows="False"
                      EnableColumnVirtualization="True"
                      EnableRowVirtualization="True"
                      HorizontalScrollBarVisibility="Visible"
                      IsReadOnly="True"
                      ItemsSource="{Binding FilteredMovieRecs, UpdateSourceTrigger=PropertyChanged}"
                      SelectionChanged="List_SelectionChanged"
                      SelectionMode="Extended"
                      SelectionUnit="FullRow"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                    >
                      <DataGrid.RowStyle>
                        <Style BasedOn="{StaticResource {x:Type DataGridRow}}" TargetType="DataGridRow">
                          <Setter Property="ContextMenu" Value="{StaticResource menuContext}" />
                        </Style>
                      </DataGrid.RowStyle>

                      <DataGrid.Columns>
                        <DataGridTemplateColumn
                          Width="314"
                          MinWidth="314"
                          CanUserResize="false"
                          Header="サムネイル"
                        >
                          <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate DataType="{x:Type local:MovieRecords}">
                              <Label
                                Height="Auto"
                                MouseDoubleClick="PlayMovie_Click"
                                MouseDown="Label_MouseDown"
                              >
                                <Image
                                  Stretch="Uniform"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.ScrollUnit="Pixel"
                                  VirtualizingPanel.VirtualizationMode="Recycling"
                                >
                                  <Image.Source>
                                    <MultiBinding
                                      Converter="{StaticResource upperTabImageSourceConverter}"
                                      ConverterParameter="{x:Static upperTabsCommon:UpperTabDecodeProfile.ListDecodePixelHeight}"
                                    >
                                      <Binding Path="ThumbPathList" />
                                      <Binding Path="IsExists" />
                                      <Binding Source="{x:Reference ListDataGrid}" Path="IsVisible" />
                                      <Binding Path="Movie_Path" />
                                      <Binding Source="{x:Reference window}" Path="UpperTabPreferredMoviePathKeysRevision" />
                                    </MultiBinding>
                                  </Image.Source>
                                </Image>
                              </Label>
                            </DataTemplate>
                          </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTemplateColumn CanUserSort="True" Header="ファイル名">
                          <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                              <VirtualizingStackPanel
                                Orientation="Vertical"
                                ScrollViewer.IsDeferredScrollingEnabled="true"
                                VirtualizingPanel.IsVirtualizing="True"
                                VirtualizingPanel.VirtualizationMode="Recycling"
                              >
                                <TextBlock
                                  FontWeight="Bold"
                                  Foreground="{DynamicResource UpperTabPanelForegroundBrush}"
                                  Text="{Binding Movie_Name, UpdateSourceTrigger=PropertyChanged}"
                                  TextWrapping="Wrap"
                                />

                                <Grid>
                                  <TextBlock
                                    Margin="0,3,0,0"
                                    Foreground="{DynamicResource UpperTabPanelForegroundBrush}"
                                    MaxHeight="34"
                                    Opacity="0.88"
                                    Text="{Binding Tag, Converter={StaticResource tagSummaryConverter}, ConverterParameter=6}"
                                    TextWrapping="Wrap">
                                    <TextBlock.Style>
                                      <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Visible" />
                                        <Style.Triggers>
                                          <Trigger Property="Text" Value="">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                          </Trigger>
                                          <DataTrigger
                                            Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}"
                                            Value="True">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                          </DataTrigger>
                                        </Style.Triggers>
                                      </Style>
                                    </TextBlock.Style>
                                  </TextBlock>

                                  <ItemsControl
                                    Height="Auto"
                                    ItemsSource="{Binding Tag, UpdateSourceTrigger=PropertyChanged, FallbackValue=''}"
                                    ScrollViewer.CanContentScroll="True"
                                    ScrollViewer.HorizontalScrollBarVisibility="Auto"
                                    ScrollViewer.IsDeferredScrollingEnabled="True"
                                    VirtualizingPanel.IsContainerVirtualizable="True"
                                    VirtualizingPanel.IsVirtualizing="True"
                                    VirtualizingPanel.ScrollUnit="Pixel"
                                    VirtualizingPanel.VirtualizationMode="Recycling"
                                  >
                                    <ItemsControl.Style>
                                      <Style TargetType="ItemsControl">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                          <Trigger Property="HasItems" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                          </Trigger>
                                          <DataTrigger
                                            Binding="{Binding RelativeSource={RelativeSource AncestorType=DataGridRow}, Path=IsSelected}"
                                            Value="False">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                          </DataTrigger>
                                        </Style.Triggers>
                                      </Style>
                                    </ItemsControl.Style>

                                    <ItemsControl.ItemTemplate>
                                      <DataTemplate>
                                        <view:TagControl DataContext="{Binding}" />
                                      </DataTemplate>
                                    </ItemsControl.ItemTemplate>

                                    <ItemsControl.ItemsPanel>
                                      <ItemsPanelTemplate>
                                        <WrapPanel
                                          MinWidth="200"
                                          MinHeight="28"
                                          MaxWidth="640"
                                          ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                          VirtualizingPanel.IsVirtualizing="True"
                                          VirtualizingPanel.VirtualizationMode="Recycling"
                                        />
                                      </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                  </ItemsControl>
                                </Grid>
                              </VirtualizingStackPanel>
                            </DataTemplate>
                          </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn
                          Width="90"
                          MinWidth="90"
                          Binding="{Binding Movie_Size, Converter={StaticResource FileSizeConverter}}"
                          CanUserSort="True"
                          Header="サイズ"
                          IsReadOnly="True"
                        >
                          <DataGridTextColumn.ElementStyle>
                            <StaticResource ResourceKey="UpperTabListCellTextStyle" />
                          </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn
                          Width="80"
                          MinWidth="80"
                          Binding="{Binding Movie_Length}"
                          CanUserSort="True"
                          Header="長さ"
                          IsReadOnly="True"
                        >
                          <DataGridTextColumn.ElementStyle>
                            <StaticResource ResourceKey="UpperTabListCellTextStyle" />
                          </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn
                          Width="70"
                          MinWidth="70"
                          Binding="{Binding Score, UpdateSourceTrigger=PropertyChanged}"
                          CanUserSort="True"
                          Header="スコア"
                          IsReadOnly="True"
                        >
                          <DataGridTextColumn.ElementStyle>
                            <StaticResource ResourceKey="UpperTabListRightAlignedCellTextStyle" />
                          </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn
                          Width="96"
                          MinWidth="96"
                          Binding="{Binding View_Count, UpdateSourceTrigger=PropertyChanged}"
                          CanUserSort="True"
                          Header="再生回数"
                          IsReadOnly="True"
                        >
                          <DataGridTextColumn.ElementStyle>
                            <StaticResource ResourceKey="UpperTabListRightAlignedCellTextStyle" />
                          </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn
                          Width="200"
                          MinWidth="200"
                          Binding="{Binding Dir, UpdateSourceTrigger=PropertyChanged}"
                          CanUserSort="True"
                          Header="フォルダ"
                          IsReadOnly="True"
                        >
                          <DataGridTextColumn.ElementStyle>
                            <StaticResource ResourceKey="UpperTabListWrappedCellTextStyle" />
                          </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn
                          Width="150"
                          MinWidth="150"
                          Binding="{Binding File_Date}"
                          CanUserReorder="False"
                          CanUserSort="True"
                          Header="ファイル日付"
                          IsReadOnly="True"
                        >
                          <DataGridTextColumn.ElementStyle>
                            <StaticResource ResourceKey="UpperTabListCellTextStyle" />
                          </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn
                          Width="150"
                          MinWidth="150"
                          Binding="{Binding Regist_Date}"
                          CanUserSort="True"
                          Header="登録日"
                          IsReadOnly="True"
                        >
                          <DataGridTextColumn.ElementStyle>
                            <StaticResource ResourceKey="UpperTabListCellTextStyle" />
                          </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn
                          Width="150"
                          MinWidth="150"
                          Binding="{Binding Last_Date, UpdateSourceTrigger=PropertyChanged}"
                          CanUserSort="True"
                          Header="最終アクセス日"
                          IsReadOnly="True"
                        >
                          <DataGridTextColumn.ElementStyle>
                            <StaticResource ResourceKey="UpperTabListCellTextStyle" />
                          </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                      </DataGrid.Columns>
                    </DataGrid>
                  </TabItem>

                  <TabItem x:Name="TabBig10" Background="{DynamicResource UpperTabBackground}" Header="5x2" Tag="120x90x5x2">
                    <ListView
                      x:Name="BigList10"
                      ItemsSource="{Binding FilteredMovieRecs, UpdateSourceTrigger=PropertyChanged}"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.IsDeferredScrollingEnabled="True"
                      SelectionChanged="List_SelectionChanged"
                      SelectionMode="Extended"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.ScrollUnit="Pixel"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                    >
                      <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                          <vwp:VirtualizingWrapPanel
                            Orientation="Horizontal"
                            StretchItems="False"
                            VirtualizingPanel.IsVirtualizing="True"
                            VirtualizingPanel.ScrollUnit="Pixel"
                            VirtualizingPanel.VirtualizationMode="Recycling"
                          />
                        </ItemsPanelTemplate>
                      </ListView.ItemsPanel>

                      <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                          <!--  内容更新時のバインドエラー回避のおまじない。  -->
                          <Setter Property="Margin" Value="0" />
                          <Setter Property="Padding" Value="0" />
                          <Setter Property="Background" Value="Transparent" />
                          <Setter Property="BorderBrush" Value="Transparent" />
                          <Setter Property="BorderThickness" Value="0" />
                          <Setter Property="HorizontalAlignment" Value="Left" />
                          <Setter Property="HorizontalContentAlignment" Value="Left" />
                          <Setter Property="VerticalContentAlignment" Value="Top" />
                          <Setter Property="Template">
                            <Setter.Value>
                              <ControlTemplate TargetType="ListViewItem">
                                <Border
                                  x:Name="ItemChrome"
                                  Padding="{TemplateBinding Padding}"
                                  HorizontalAlignment="Left"
                                  Background="Transparent"
                                  BorderBrush="Transparent"
                                  BorderThickness="1"
                                  SnapsToDevicePixels="True"
                                >
                                  <ContentPresenter
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"
                                  />
                                </Border>

                                <ControlTemplate.Triggers>
                                  <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="ItemChrome" Property="Background" Value="#123A6E8A" />
                                    <Setter TargetName="ItemChrome" Property="BorderBrush" Value="#803A9AD9" />
                                  </Trigger>
                                  <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="ItemChrome" Property="Background" Value="#203A6E8A" />
                                    <Setter TargetName="ItemChrome" Property="BorderBrush" Value="#FF3A9AD9" />
                                  </Trigger>
                                  <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                      <Condition Property="IsSelected" Value="True" />
                                      <Condition Property="Selector.IsSelectionActive" Value="False" />
                                    </MultiTrigger.Conditions>
                                    <Setter TargetName="ItemChrome" Property="Background" Value="#163A6E8A" />
                                    <Setter TargetName="ItemChrome" Property="BorderBrush" Value="#AA3A9AD9" />
                                  </MultiTrigger>
                                </ControlTemplate.Triggers>
                              </ControlTemplate>
                            </Setter.Value>
                          </Setter>
                          <!--  ここを追加: ListViewItem内のどこをクリックしても選択状態にする  -->
                          <EventSetter
                            Event="PreviewMouseLeftButtonDown"
                            Handler="BigList10Item_PreviewMouseLeftButtonDown"
                          />
                        </Style>
                      </ListView.ItemContainerStyle>

                      <ListView.ItemTemplate>
                        <DataTemplate>
                          <Grid Margin="0" HorizontalAlignment="Left">
                            <Grid.RowDefinitions>
                              <RowDefinition Height="Auto" />
                              <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <view:BigTitleControl Grid.Row="0" MaxWidth="820" DataContext="{Binding}" />

                            <Grid Grid.Row="1">
                              <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                              </Grid.ColumnDefinitions>

                              <Label
                                Grid.Column="0"
                                Width="600"
                                Height="180"
                                Padding="0"
                                BorderThickness="0"
                                MouseDoubleClick="PlayMovie_Click"
                                MouseDown="Label_MouseDown"
                              >
                                <Image
                                  ContextMenu="{StaticResource menuContext}"
                                  Stretch="Uniform"
                                  VirtualizingPanel.IsVirtualizing="True"
                                  VirtualizingPanel.ScrollUnit="Pixel"
                                  VirtualizingPanel.VirtualizationMode="Recycling"
                                >
                                  <Image.Source>
                                    <MultiBinding
                                      Converter="{StaticResource upperTabImageSourceConverter}"
                                      ConverterParameter="{x:Static upperTabsCommon:UpperTabDecodeProfile.Big10DecodePixelHeight}"
                                    >
                                      <Binding Path="ThumbPathBig10" />
                                      <Binding Path="IsExists" />
                                      <Binding Source="{x:Reference BigList10}" Path="IsVisible" />
                                      <Binding Path="Movie_Path" />
                                      <Binding Source="{x:Reference window}" Path="UpperTabPreferredMoviePathKeysRevision" />
                                    </MultiBinding>
                                  </Image.Source>
                                </Image>
                              </Label>

                              <view:BigDetailControl Grid.Column="1" Width="130" HorizontalAlignment="Left" DataContext="{Binding}" />
                            </Grid>
                          </Grid>
                        </DataTemplate>
                      </ListView.ItemTemplate>
                    </ListView>
                  </TabItem>

                <TabItem x:Name="TabThumbnailError" Header="サムネ救済">
                    <rescueTab:RescueTabView
                      BulkNormalRetryRequested="UpperTabRescueBulkNormalRetryButton_Click"
                      x:Name="UpperTabRescueViewHost"
                      PlayRequested="UpperTabRescuePlayRequested"
                      RefreshRequested="UpperTabRescueRefreshButton_Click"
                      SelectedAutogenRequested="UpperTabRescueSelectedAutogenButton_Click"
                      SelectedFfmpegRequested="UpperTabRescueSelectedFfmpegButton_Click"
                      SelectedFfmediaToolkitRequested="UpperTabRescueSelectedFfmediaToolkitButton_Click"
                      SelectedOpenCvRequested="UpperTabRescueSelectedOpenCvButton_Click"
                      SelectedIndexRepairRequested="UpperTabRescueSelectedIndexRepairButton_Click"
                      SelectedBlackConfirmRequested="UpperTabRescueSelectedBlackConfirmButton_Click"
                      SelectedBlackDeepRetryRequested="UpperTabRescueSelectedBlackDeepRetryButton_Click"
                      SelectedBlackLiteRetryRequested="UpperTabRescueSelectedBlackLiteRetryButton_Click"
                      RescueListSelectionChangedRequested="UpperTabRescueListSelectionChanged"/>
                  </TabItem>

                  <TabItem x:Name="TabDuplicateVideos" Header="重複動画">
                    <duplicateVideosTab:DuplicateVideosTabView
                      x:Name="UpperTabDuplicateVideosViewHost"
                      DetectRequested="UpperTabDuplicateVideosDetectButton_Click"
                      DetailCellEditEndingRequested="UpperTabDuplicateVideosDetailCellEditEnding"
                      DetailPlayRequested="UpperTabDuplicateVideosPlayRequested"
                      DetailSelectionChangedRequested="UpperTabDuplicateVideosDetailSelectionChanged"
                      GroupSelectionChangedRequested="UpperTabDuplicateVideosGroupSelectionChanged"
                      GroupSortSelectionChangedRequested="UpperTabDuplicateVideosGroupSortSelectionChanged" />
                  </TabItem>
                  </TabControl>

                  <!-- 外部 WhiteBrowser スキン時だけ、既存タブを隠して別 host を前面へ出す。 -->
                  <ContentControl
                    x:Name="ExternalSkinHostPresenter"
                    Background="{DynamicResource MaterialDesignPaper}"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    Visibility="Collapsed" />
                </Grid>
              </avalonDock:LayoutDocument>
              <!--  ドキュメント ★ここまで  -->
            </avalonDock:LayoutDocumentPane>
          </avalonDock:LayoutPanel>
          <avalonDock:LayoutAnchorablePane x:Name="uxAnchorablePane2" DockHeight="240">
            <!--  ツールウィンドウ  -->
            <!--  何故かクローズ（非表示なんだけど）閉じて保存すると二度と表示されないのでクローズ禁止にした  -->
            <avalonDock:LayoutAnchorable
              x:Name="TagEditorBottomTab"
              Title="タグ"
              AutoHideHeight="200"
              CanAutoHide="True"
              CanClose="False"
              CanDockAsTabbedDocument="False"
              CanHide="False"
              ContentId="ToolTagEditor"
            >
              <tagEditorTab:TagEditorTabView x:Name="TagEditorTabViewHost" />
            </avalonDock:LayoutAnchorable>

            <avalonDock:LayoutAnchorable
              x:Name="exDetail"
              Title="詳細"
              AutoHideHeight="200"
              CanAutoHide="True"
              CanClose="False"
              CanDockAsTabbedDocument="False"
              CanHide="False"
              ContentId="ToolExtension"
            >
              <extensionTab:ExtensionTabView x:Name="ExtensionTabViewHost" />
            </avalonDock:LayoutAnchorable>

            <avalonDock:LayoutAnchorable
              x:Name="exBookMark"
              Title="ブックマーク"
              AutoHideHeight="200"
              CanAutoHide="True"
              CanClose="False"
              CanDockAsTabbedDocument="False"
              CanHide="False"
              ContentId="ToolBookmark"
            >
              <bookmarkTab:BookmarkTabView x:Name="BookmarkTabViewHost" DataContext="{Binding}" />
            </avalonDock:LayoutAnchorable>
            <!--  ツールウィンドウ  -->
            <avalonDock:LayoutAnchorable
              x:Name="TagBar"
              Title="保存済み検索条件"
              AutoHideHeight="200"
              CanAutoHide="True"
              CanClose="False"
              CanDockAsTabbedDocument="False"
              CanHide="False"
              ContentId="ToolTagBar"
            >
              <savedSearchTab:SavedSearchTabView x:Name="SavedSearchTabViewHost" />
            </avalonDock:LayoutAnchorable>

            <avalonDock:LayoutAnchorable
              x:Name="ThumbnailProgressTab"
              Title="サムネイル進捗"
              AutoHideHeight="200"
              CanAutoHide="True"
              CanClose="False"
              CanDockAsTabbedDocument="False"
              CanHide="False"
              IsSelected="True"
              ContentId="ToolThumbnailProgress"
            >
              <thumbnailProgressTab:ThumbnailProgressTabView
                x:Name="ThumbnailProgressTabViewHost"
                DataContext="{Binding}"
                GpuDecodeEnabledClicked="ThumbnailProgressGpuDecodeEnabled_Click"
                MissingThumbnailScanRequested="ThumbnailProgressMissingScan_Click"
                ParallelismSliderValueChanged="ThumbnailProgressParallelismSlider_ValueChanged"
                PresetFastRequested="ThumbnailProgressPresetFastButton_Click"
                PresetMaxRequested="ThumbnailProgressPresetMaxButton_Click"
                PresetLowLoadRequested="ThumbnailProgressPresetLowLoadButton_Click"
                PresetNormalRequested="ThumbnailProgressPresetNormalButton_Click"
                PresetSsdRequested="ThumbnailProgressPresetSsdButton_Click"
                SlowLaneMinGbSliderValueChanged="ThumbnailProgressSlowLaneMinGbSlider_ValueChanged"
              />
            </avalonDock:LayoutAnchorable>

            <avalonDock:LayoutAnchorable
              x:Name="ThumbnailErrorBottomTab"
              Title="サムネ失敗Debug"
              AutoHideHeight="200"
              CanAutoHide="True"
              CanClose="False"
              CanDockAsTabbedDocument="False"
              CanHide="False"
              ContentId="ToolThumbnailError"
            >
              <thumbnailErrorTab:ThumbnailErrorTabView
                x:Name="ThumbnailErrorBottomTabViewHost"
                ClearListRequested="ClearThumbnailErrorListButton_Click"
                DataContext="{Binding}"
                ErrorListSelectionChangedRequested="List_SelectionChanged"
                ReloadRequested="ReloadThumbnailErrorListButton_Click"
                RescueAllRequested="RescueAllThumbnailErrorsButton_Click"
                RescueSelectedRequested="RescueSelectedThumbnailErrorsButton_Click"
              />
            </avalonDock:LayoutAnchorable>

            <avalonDock:LayoutAnchorable
              x:Name="DebugTab"
              Title="Debug"
              AutoHideHeight="200"
              CanAutoHide="True"
              CanClose="False"
              CanDockAsTabbedDocument="False"
              CanHide="True"
              ContentId="ToolDebug"
            >
              <debugTab:DebugTabView
                x:Name="DebugTabViewHost"
                ClearCurrentDbRecordsRequested="DebugClearCurrentDbRecords_Click"
                ClearQueueDbRecordsRequested="DebugClearQueueDbRecords_Click"
                DeleteCurrentDbRequested="DebugDeleteCurrentDb_Click"
                DeleteQueueDbRequested="DebugDeleteQueueDb_Click"
                DeleteThumbnailDirRequested="DebugDeleteThumbnailDir_Click"
                OpenAppDataDirRequested="DebugOpenAppDataDir_Click"
                OpenCurrentDirRequested="DebugOpenCurrentDir_Click"
                OpenThumbDirRequested="DebugOpenThumbDir_Click"
                OpenDbDirRequested="DebugOpenDbDir_Click"
                OpenLogDirRequested="DebugOpenLogDir_Click"
                OpenFailureDbDirRequested="DebugOpenFailureDbDir_Click"
                ClearFailureDbRecordsRequested="DebugClearFailureDbRecords_Click"
                DeleteFailureDbRequested="DebugDeleteFailureDb_Click"
                RefreshFailureDbRecordCountRequested="DebugRefreshFailureDbRecordCount_Click"
                OpenCurrentDbDirRequested="DebugOpenCurrentDbDir_Click"
                OpenQueueDbDirRequested="DebugOpenQueueDbDir_Click"
                OpenThumbnailDirRequested="DebugOpenThumbnailDir_Click"
                RefreshCurrentDbRecordCountRequested="DebugRefreshCurrentDbRecordCount_Click"
                RefreshQueueDbRecordCountRequested="DebugRefreshQueueDbRecordCount_Click"
                RecreateAllThumbnailsRequested="DebugRecreateAllThumbnails_Click"
              />
            </avalonDock:LayoutAnchorable>

            <avalonDock:LayoutAnchorable
              x:Name="LogTab"
              Title="Log"
              AutoHideHeight="200"
              CanAutoHide="True"
              CanClose="False"
              CanDockAsTabbedDocument="False"
              CanHide="True"
              ContentId="ToolLog"
            >
              <logTab:LogTabView
                x:Name="LogTabViewHost"
                OpenLogDirRequested="DebugOpenLogDir_Click"
                RefreshPreviewRequested="LogRefreshPreview_Click"
                SwitchChangedRequested="LogTabSwitchChanged_Click"
              />
            </avalonDock:LayoutAnchorable>
          </avalonDock:LayoutAnchorablePane>
        </avalonDock:LayoutPanel>
      </avalonDock:LayoutRoot>
    </avalonDock:DockingManager>

    <materialDesign:DrawerHost
      x:Name="MainDrawerHost"
      IsLeftDrawerOpen="{Binding IsChecked, ElementName=MenuToggleButton}"
    >
      <materialDesign:DrawerHost.LeftDrawerContent>
        <DockPanel
          Width="208"
          Background="{DynamicResource NavigationDrawerBackgroundBrush}"
        >
          <ToggleButton
            Margin="12,10"
            HorizontalAlignment="Right"
            DockPanel.Dock="Top"
            IsChecked="{Binding IsChecked, ElementName=MenuToggleButton, Mode=TwoWay}"
            Style="{StaticResource AppHamburgerToggleButtonStyle}"
          />

          <ScrollViewer
            HorizontalScrollBarVisibility="Disabled"
            VerticalScrollBarVisibility="Auto"
          >
            <StackPanel Margin="12,0,12,12">
              <Button
                x:Name="BtnNew"
                Click="BtnNew_Click"
                Content="新規作成"
                Style="{StaticResource LeftDrawerButtonStyle}"
              />

              <Button
                x:Name="BtnOpen"
                Click="BtnOpen_Click"
                Content="ファイルを開く"
                Style="{StaticResource LeftDrawerButtonStyle}"
              />

              <Border Style="{StaticResource LeftDrawerSeparatorStyle}" />

              <TreeView
                x:Name="MenuRecent"
                ItemContainerStyle="{StaticResource LeftDrawerTreeItemStyle}"
                ItemsSource="{Binding RecentTreeRoot, UpdateSourceTrigger=PropertyChanged}"
                Style="{StaticResource AppTreeViewStyle}"
              >
                <TreeView.ItemTemplate>
                  <HierarchicalDataTemplate
                    DataType="{x:Type local:TreeSource}"
                    ItemContainerStyle="{StaticResource LeftDrawerTreeItemStyle}"
                    ItemsSource="{Binding Children, UpdateSourceTrigger=PropertyChanged}"
                  >
                    <Button
                      Click="MenuRecentTree_Click"
                      Style="{StaticResource LeftDrawerButtonStyle}"
                      Tag="{Binding Text}"
                    >
                      <TextBlock
                        Style="{StaticResource LeftDrawerTextStyle}"
                        Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                        ToolTip="{Binding Text}"
                      />
                    </Button>
                  </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
              </TreeView>

              <Border Style="{StaticResource LeftDrawerSeparatorStyle}" />

              <TreeView
                x:Name="MenuConfig"
                ItemContainerStyle="{StaticResource LeftDrawerTreeItemStyle}"
                ItemsSource="{Binding ConfigTreeRoot, UpdateSourceTrigger=PropertyChanged}"
                Style="{StaticResource AppTreeViewStyle}"
              >
                <TreeView.ItemTemplate>
                  <HierarchicalDataTemplate
                    DataType="{x:Type local:TreeSource}"
                    ItemContainerStyle="{StaticResource LeftDrawerTreeItemStyle}"
                    ItemsSource="{Binding Children, UpdateSourceTrigger=PropertyChanged}"
                  >
                    <Button
                      Click="MenuBtnSettings_Click"
                      Style="{StaticResource LeftDrawerButtonStyle}"
                      Tag="{Binding Text}"
                    >
                      <TextBlock
                        Style="{StaticResource LeftDrawerTextStyle}"
                        Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                        ToolTip="{Binding Text}"
                      />
                    </Button>
                  </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
              </TreeView>

              <Border Style="{StaticResource LeftDrawerSeparatorStyle}" />

              <TreeView
                x:Name="MenuTool"
                ItemContainerStyle="{StaticResource LeftDrawerTreeItemStyle}"
                ItemsSource="{Binding ToolTreeRoot, UpdateSourceTrigger=PropertyChanged}"
                Style="{StaticResource AppTreeViewStyle}"
              >
                <TreeView.ItemTemplate>
                  <HierarchicalDataTemplate
                    DataType="{x:Type local:TreeSource}"
                    ItemContainerStyle="{StaticResource LeftDrawerTreeItemStyle}"
                    ItemsSource="{Binding Children, UpdateSourceTrigger=PropertyChanged}"
                  >
                    <Button
                      Click="MenuBtnTool_Click"
                      Style="{StaticResource LeftDrawerButtonStyle}"
                      Tag="{Binding Text}"
                    >
                      <TextBlock
                        Style="{StaticResource LeftDrawerTextStyle}"
                        Text="{Binding Text, UpdateSourceTrigger=PropertyChanged}"
                        ToolTip="{Binding Text}"
                      />
                    </Button>
                  </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
              </TreeView>

              <Border Style="{StaticResource LeftDrawerSeparatorStyle}" />

              <Button
                x:Name="BtnExit"
                Click="BtnExit_Click"
                Content="終了"
                Style="{StaticResource LeftDrawerButtonStyle}"
              />
            </StackPanel>
          </ScrollViewer>
        </DockPanel>
      </materialDesign:DrawerHost.LeftDrawerContent>

      <Grid>
        <Grid.RowDefinitions>
          <RowDefinition Height="48" />
          <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <materialDesign:ColorZone
          x:Name="MainHeaderBar"
          Grid.Row="0"
          Padding="10,6"
          Mode="Custom"
          Background="{DynamicResource MainHeaderBackground}"
          Foreground="{DynamicResource MainHeaderForegroundBrush}"
          VerticalAlignment="Top"
        >
        <Grid VerticalAlignment="Center">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
          </Grid.ColumnDefinitions>

          <ToggleButton
            x:Name="MenuToggleButton"
            Grid.Column="0"
            AutomationProperties.Name="HamburgerToggleButton"
            Width="32"
            Height="32"
            Margin="0,2,12,2"
            Checked="MenuToggleButton_Checked"
            Unchecked="MenuToggleButton_Unchecked"
            VerticalAlignment="Center"
            Style="{DynamicResource AppHamburgerToggleButtonStyle}"
          />

          <Grid Grid.Column="1" VerticalAlignment="Center">
            <Grid
              x:Name="MainHeaderStandardChromePanel"
              Height="36"
              VerticalAlignment="Center"
            >
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" MinWidth="180" MaxWidth="326" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" MinWidth="0" />
                <ColumnDefinition Width="Auto" />
              </Grid.ColumnDefinitions>
              <ComboBox
                x:Name="SearchBox"
                Grid.Column="0"
                Margin="0,0,10,0"
                Padding="4,3,0,3"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                VerticalAlignment="Center"
                materialDesign:HintAssist.Hint="検索キーワードを入れて下さい。"
                materialDesign:HintAssist.IsFloating="True"
                materialDesign:TextFieldAssist.DecorationVisibility="Hidden"
                materialDesign:TextFieldAssist.HasClearButton="True"
                materialDesign:TextFieldAssist.HasOutlinedTextField="True"
                materialDesign:TextFieldAssist.TextFieldCornerRadius="3"
                materialDesign:TextFieldAssist.UnderlineBrush="{DynamicResource MaterialDesignBody}"
                Background="{DynamicResource MainHeaderSearchBoxBackground}"
                Cursor=""
                DisplayMemberPath="Find_Text"
                DockPanel.Dock="Top"
                DropDownClosed="SearchBox_DropDownClosed"
                FontSize="13"
                Foreground="{DynamicResource MainHeaderInputForegroundBrush}"
                IsEditable="True"
                ItemsSource="{Binding HistoryRecs}"
                LostFocus="SearchBox_LostFocus"
                PreviewKeyDown="SearchBox_PreviewKeyDown"
                SelectedValue="{Binding DbInfo.SearchKeyword}"
                SelectedValuePath="Find_Text"
                SelectionChanged="SearchBox_SelectionChanged"
                SelectiveScrollingGrid.SelectiveScrollingOrientation="None"
                Style="{StaticResource AppSearchComboBoxStyle}"
                Text="{Binding DbInfo.SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                TextBoxBase.TextChanged="SearchBox_TextChanged"
              >
                <ComboBox.ItemContainerStyle>
                  <Style BasedOn="{StaticResource AppComboBoxItemStyle}" TargetType="ComboBoxItem">
                    <!--  内容更新時のバインドエラー回避のおまじない。  -->
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="VerticalContentAlignment" Value="Stretch" />
                    <EventSetter Event="MouseMove" Handler="SearchBoxItem_MouseMove" />
                    <EventSetter
                      Event="PreviewMouseLeftButtonDown"
                      Handler="SearchBoxItem_PreviewMouseLeftButtonDown"
                    />
                    <Style.Triggers>
                      <!--  選択時の反転表示  -->
                      <Trigger Property="IsSelected" Value="True" />
                    </Style.Triggers>
                  </Style>
                </ComboBox.ItemContainerStyle>
              </ComboBox>
              <Button
                x:Name="BulkTagAssignButton"
                Grid.Column="1"
                Margin="0,0,10,0"
                Click="BulkTagAssignButton_Click"
                Content="タグ付..."
                Style="{StaticResource AppHeaderButtonStyle}"
                ToolTip="現在の検索キーワードをタグとして、表示中の動画または選択中の動画へ追加します"
              />
              <Button
                x:Name="ReloadButton"
                Grid.Column="2"
                Click="ReloadButton_Click"
                Content="再読込"
                IsDefault="True"
                Style="{StaticResource AppHeaderButtonStyle}"
                ToolTip="一覧再読込と監視フォルダ更新チェックを実行します"
              />
              <Label
                Grid.Column="3"
                Margin="18,0,0,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Content="ソート順："
                FontSize="13"
                Foreground="{DynamicResource MainHeaderForegroundBrush}"
              />
              <ComboBox
                x:Name="ComboSort"
                Grid.Column="4"
                Width="160"
                Margin="4,2,4,2"
                VerticalAlignment="Center"
                Background="{DynamicResource MainHeaderSortComboBackground}"
                FontSize="13"
                DisplayMemberPath="Name"
                Foreground="{DynamicResource MainHeaderInputForegroundBrush}"
                ItemsSource="{Binding SortLists}"
                SelectedValue="{Binding DbInfo.Sort}"
                SelectedValuePath="Id"
                SelectionChanged="ComboSort_SelectionChanged"
                Style="{StaticResource AppCompactComboBoxStyle}"
              />
              <Label
                Grid.Column="5"
                Margin="14,0,0,0"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Content="スキン："
                FontSize="13"
                Foreground="{DynamicResource MainHeaderForegroundBrush}"
              />
              <Grid Grid.Column="6" VerticalAlignment="Center">
                <!-- ヘッダーは差し替えず、スキン切替だけを共通ヘッダー上で完結させる。 -->
                <ComboBox
                  x:Name="ExternalSkinMinimalSkinSelector"
                  Width="170"
                  VerticalAlignment="Center"
                  Background="{DynamicResource MainHeaderSortComboBackground}"
                  DisplayMemberPath="Name"
                  Foreground="{DynamicResource MainHeaderInputForegroundBrush}"
                  SelectedValuePath="Name"
                  SelectionChanged="ExternalSkinMinimalSkinSelector_SelectionChanged"
                  Style="{StaticResource AppCompactComboBoxStyle}"
                  TextSearch.TextPath="Name"
                />
                <TextBlock
                  x:Name="ExternalSkinMinimalSkinNameText"
                  VerticalAlignment="Center"
                  FontSize="13"
                  Foreground="{DynamicResource MainHeaderForegroundBrush}"
                  TextTrimming="CharacterEllipsis"
                  Visibility="Collapsed"
                />
              </Grid>
              <TextBlock
                Grid.Column="7"
                Margin="18,0,0,0"
                VerticalAlignment="Center"
                FontSize="13"
                Foreground="{DynamicResource MainHeaderForegroundBrush}">
                <Run Text="表示 / 登録  ：  " />
                <Run Text="{Binding DbInfo.SearchCount, Mode=OneWay}" />
                <Run Text=" / " />
                <Run
                  Text="{Binding DbInfo.RegisteredMovieCount, Mode=OneWay}"
                />
              </TextBlock>
              <TextBlock
                x:Name="lbDbFullPath"
                Grid.Column="8"
                Margin="14,0,0,0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                FontSize="13"
                Foreground="{DynamicResource MainHeaderForegroundBrush}"
                Text="{Binding DbInfo.DBFullPath, UpdateSourceTrigger=PropertyChanged}"
                TextTrimming="CharacterEllipsis"
              />
              <Border
                x:Name="ExternalSkinFallbackNoticeBorder"
                Grid.Column="9"
                Margin="14,0,0,0"
                MaxWidth="420"
                Padding="10,4"
                VerticalAlignment="Center"
                Background="#26FFB300"
                BorderBrush="#66FFB300"
                BorderThickness="1"
                CornerRadius="4"
                Visibility="Collapsed"
              >
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" MinWidth="0" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                  </Grid.ColumnDefinitions>
                  <materialDesign:PackIcon
                    Grid.Column="0"
                    Width="16"
                    Height="16"
                    VerticalAlignment="Center"
                    Kind="AlertOutline"
                  />
                  <TextBlock
                    x:Name="ExternalSkinFallbackNoticeText"
                    Grid.Column="1"
                    Margin="8,0,0,0"
                    VerticalAlignment="Center"
                    FontSize="12"
                    Foreground="{DynamicResource MainHeaderForegroundBrush}"
                    TextTrimming="CharacterEllipsis"
                  />
                  <Button
                    x:Name="ExternalSkinFallbackRetryButton"
                    Grid.Column="2"
                    Margin="10,0,0,0"
                    Padding="8,2"
                    Click="ExternalSkinFallbackRetryButton_Click"
                    Content="再試行"
                    Style="{StaticResource AppHeaderButtonStyle}"
                  />
                  <Button
                    x:Name="ExternalSkinFallbackOpenRuntimeDownloadButton"
                    Grid.Column="3"
                    Margin="8,0,0,0"
                    Padding="8,2"
                    Click="ExternalSkinFallbackOpenRuntimeDownloadButton_Click"
                    Content="Runtimeを入手"
                    Style="{StaticResource AppHeaderButtonStyle}"
                    Visibility="Collapsed"
                  />
                  <Button
                    x:Name="ExternalSkinFallbackOpenLogButton"
                    Grid.Column="4"
                    Margin="8,0,0,0"
                    Padding="8,2"
                    Click="ExternalSkinFallbackOpenLogButton_Click"
                    Content="ログを開く"
                    Style="{StaticResource AppHeaderButtonStyle}"
                  />
                </Grid>
              </Border>
            </Grid>

            <!-- 互換テスト用の旧最小シェル。実表示では通常ヘッダーを差し替えない。 -->
            <DockPanel
              x:Name="ExternalSkinMinimalChromePanel"
              LastChildFill="True"
              Visibility="Collapsed"
            >
              <StackPanel
                DockPanel.Dock="Right"
                Orientation="Horizontal"
                VerticalAlignment="Center"
              >
                <Button
                  x:Name="ExternalSkinMinimalReloadButton"
                  Margin="0,0,8,0"
                  Click="ExternalSkinMinimalReloadButton_Click"
                  Content="再読込"
                  Style="{StaticResource AppHeaderButtonStyle}"
                />
                <Button
                  x:Name="ExternalSkinBackToGridButton"
                  Margin="0,0,8,0"
                  Click="ExternalSkinBackToGridButton_Click"
                  Content="Gridへ戻る"
                  Style="{StaticResource AppHeaderButtonStyle}"
                />
                <Button
                  x:Name="ExternalSkinMinimalSettingsButton"
                  Click="ExternalSkinMinimalSettingsButton_Click"
                  Content="設定"
                  Style="{StaticResource AppHeaderButtonStyle}"
                />
              </StackPanel>

              <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <materialDesign:PackIcon
                  Width="18"
                  Height="18"
                  VerticalAlignment="Center"
                  Kind="PaletteSwatch"
                />
                <TextBlock
                  Margin="8,0,0,0"
                  VerticalAlignment="Center"
                  FontSize="13"
                  Foreground="{DynamicResource MainHeaderForegroundBrush}"
                  Text="外部スキン"
                />
                <Border
                  Width="1"
                  Height="18"
                  Margin="12,0"
                  VerticalAlignment="Center"
                  Background="{DynamicResource MainHeaderForegroundBrush}"
                  Opacity="0.35"
                />
                <TextBlock
                  VerticalAlignment="Center"
                  FontSize="13"
                  Foreground="{DynamicResource MainHeaderForegroundBrush}"
                  Text="{Binding DbInfo.DBName, UpdateSourceTrigger=PropertyChanged}"
                />
              </StackPanel>
            </DockPanel>
          </Grid>
        </Grid>
        </materialDesign:ColorZone>
      </Grid>
    </materialDesign:DrawerHost>

    <notifications:NotificationArea
      x:Name="ProgressArea"
      Position="BottomRight"
      Language="ja-JP"
      TextElement.FontFamily="Yu Gothic UI"
    />

  </Grid>
</Window>
